/*
import Foundation

public class FFL {
    public static func initialize() {
        let config = FFLConfig(serverConfig: .dev)
        FlipFlopLite.initialize(config: config)
    }

    public static func getStreamer(accessToken: String) -> FFLStreamer {
        return FlipFlopLite.FFLStreamer(accessToken: accessToken)
    }

    public static func getLivePlayer(accessToken: String, videoRoomId: UInt64, channelId: UInt64) -> FFLLivePlayer {
        return FlipFlopLite.FFLLivePlayer(accessToken: accessToken, videoRoomId: videoRoomId, channelId: channelId)
    }

    public static func getVodPlayer(accessToken: String, channelId: UInt64, liveStartedAt: String) -> FFLVodPlayer {
        return FlipFlopLite.FFLVodPlayer(accessToken: accessToken, channelId: channelId, liveStartedAt: liveStartedAt)
    }
}*/


